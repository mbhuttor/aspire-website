---
import Layout from '~/layouts/PageLayout.astro';
title: "Facilities";
---

<script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js" client:load></script>
<Layout style="background: #fdfcf7; will-change: transform;">
<section class="facilities-section">
    <div class="container">
      <h1 class="facilities-title">Our Campus Facilities</h1>
      <p class="facilities-subtitle">
        Aspire College Gamber offers a welcoming and well-equipped campus to help students learn, grow, and thrive.
      </p>

      <!-- Academic Facilities -->
      <div class="facilities-category">
        <h2>Academic Facilities</h2>
        <div class="facilities-grid">
          <div class="facility-card">
            <iconify-icon icon="tabler:building" class="facility-icon spin" />
            <div class="facility-title">Modern Classrooms</div>
            <div class="facility-desc">Smart, well-lit rooms for interactive learning.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:device-desktop" class="facility-icon pulse" />
            <div class="facility-title">Computer Labs</div>
            <div class="facility-desc">Fast internet, updated systems, and tech access.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:books" class="facility-icon bounce" />
            <div class="facility-title">Library</div>
            <div class="facility-desc">Quiet space with academic books and study materials.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:flask" class="facility-icon wiggle" />
            <div class="facility-title">Science Labs</div>
            <div class="facility-desc">Physics, Chemistry, and Biology labs with essential equipment.</div>
          </div>
        </div>
      </div>

      <!-- Student Life -->
      <div class="facilities-category">
        <h2>Student Life</h2>
        <div class="facilities-grid">
          <div class="facility-card">
            <iconify-icon icon="tabler:cup" class="facility-icon bounce" />
            <div class="facility-title">Cafeteria</div>
            <div class="facility-desc">Nutritious meals and snacks in a clean environment.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:moon" class="facility-icon pulse" />
            <div class="facility-title">Prayer Area</div>
            <div class="facility-desc">Separate, quiet spaces for spiritual reflection and prayer.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:armchair" class="facility-icon wiggle" />
            <div class="facility-title">Student Lounge</div>
            <div class="facility-desc">A relaxing space for students to unwind and connect.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:drone" class="facility-icon spin" />
            <div class="facility-title">Virtual Tour</div>
            <div class="facility-desc">Explore the campus remotely—coming soon.</div>
          </div>
        </div>
      </div>

      <!-- Sports & Support -->
      <div class="facilities-category">
        <h2>Sports & Support</h2>
        <div class="facilities-grid">
          <div class="facility-card">
            <iconify-icon icon="tabler:soccer-field" class="facility-icon pulse" />
            <div class="facility-title">Outdoor Ground</div>
            <div class="facility-desc">Spacious field for sports and physical activities.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:heart-rate-monitor" class="facility-icon bounce" />
            <div class="facility-title">Counseling Room</div>
            <div class="facility-desc">Private support for students' mental well-being.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:shield-check" class="facility-icon wiggle" />
            <div class="facility-title">Security</div>
            <div class="facility-desc">Surveillance and secure entry for safety.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:ambulance" class="facility-icon bounce" />
            <div class="facility-title">First Aid</div>
            <div class="facility-desc">On-site medical assistance for quick care.</div>
          </div>
          <div class="facility-card">
            <iconify-icon icon="tabler:pizza" class="facility-icon spin" />
            <div class="facility-title">Pizza Spot</div>
            <div class="facility-desc">Delicious pizza coming soon to campus!</div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap');
html, body {
    font-family: 'Inter', sans-serif;
    background: #fdfcf7;
    color: black;
    min-height: 100vh;
    overflow-x: hidden;
    position: relative;
    z-index: 0;
}

body {
    background: #fdfcf7;
    color: black;
    overflow-x: hidden;
    min-height: 100vh;
    position: relative;
    z-index: 0;
}

  header {
    background: #fdfcf7 !important;
    color: black;
}
.facilities-section {
    background: #fdfcf7;
    padding: 4rem 1rem;
    color: black;
}

  .container {
    max-width: 1200px;
    margin: 0 auto;
    text-align: center;
  }

  .facilities-title {
    font-size: 2.75rem;
    font-weight: 800;
    color: #1e293b;
  }

  .facilities-subtitle {
    font-size: 1.2rem;
    color: #4b5563;
    margin: 1rem auto 3rem;
    max-width: 700px;
  }

  .facilities-category {
    margin-bottom: 4rem;
    text-align: left;
  }

  .facilities-category h2 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: #1e40af;
    text-align: center;
  }

  .facilities-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5rem;
    padding: 1rem;
  }

  .facility-card {
    background: white !important;
    color: black !important;
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: 0 6px 16px rgba(0,0,0,0.05);
    text-align: center;
}

  .facility-icon {
    font-size: 2rem;
    color: #1e40af;
    margin-bottom: 0.75rem;
  }

  .facility-title {
    font-weight: 700;
    color: black !important;
    margin-bottom: 0.5rem;
    font-size: 1.1rem;
    opacity: 1 !important;
}

  .facility-desc {
    color: black !important;
    font-size: 0.95rem;
    opacity: 1 !important;
}

  .spin {
    animation: spin 3s linear infinite;
  }

  .pulse {
    animation: pulse 1.5s ease-in-out infinite;
  }

  .bounce {
    animation: bounce 1s infinite;
  }

  .wiggle {
    animation: wiggle 1s ease-in-out infinite;
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-5px); }
  }

  @keyframes wiggle {
    0%, 100% { transform: rotate(0deg); }
    25% { transform: rotate(3deg); }
    75% { transform: rotate(-3deg); }
  }
  :root, #app, main, .page-wrapper, .layout, #__astro, #__next, .astro-root {
    background: #fdfcf7 !important;
    color: black;
}
  * {
    backface-visibility: visible !important;
    transform: translateZ(0);
  }
</style>
